객체는 클래스이다 : X
클래스는 객체이다 : O
(클래스는 객체의 일부분)


클래스, 객체, 생성자, 메소드, 제네릭, 컬렉션, 인덱스, 오버로딩, 오버라이딩....


제네릭은 <>(= 다이아몬드 연산자라고 부름)
제네릭은 타입을 가둔두는 것이다.. 가두는 이유는 여러 개발자가 동시 작업할 때 이 공간엔 이것만 들어가도록 지정하기 위해서이다.

클래스도 자료형임. 클래스 = 타입임. 그래서 List<>에서 <>안에 클래스명 넣을 수 있음.


Application2 -> main 먼저 보기 -> WildcardFarm이란 공간 만듦. -> new하면 WildcardFarm 안 3가지 메소드 사용 준비 완(anyType,  extendsType, superType)
-> wfarm.anyType(new RabbitFarm<Rabbit>(new Rabbit()));  에서 1번 new Rabbit(). 토끼 생김 -> 2. new RabbitFarm<Rabbit>(..) 농장에 토끼 넣음 
-> WildcardFarm 클래스에서 RabbitFarm<?> farm을 농장 안에 들어간 토끼랑 퉁침.
-> farm에서 . 찍는 순간 레빗으로 들어가는거임.
-> 레빗 안에는 크라이 있음. 크라이 불러옴.

==================================
제네릭은 컬렉션 사용할 때만 사용.

map은 중복값 허용. 키 값만 다르면 OK
set은 중복값 허용하지 않음.
list 배열 한계 극복함. 배열은 공간 지정하지만 리스트는 지정하지 않음.

ArrayList & map = 99%
이 둘이 중요하고 잘 알기
