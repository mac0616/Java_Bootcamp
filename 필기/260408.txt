Application 영역 : 	       entity 생성
Persistence Context 영역 :    manager 통해서 persist()로 전송
==============================================
manager.find(엔티티, @Id) => 

영속성에서 가장 중요한 것은 키값

==============================================
App ---- PC ---> DB
App <--- PC ---- DB
=> 무조건 Persistence Context 를 거침.
대부분의 실수는 저기서 나옴.  

==============================================
DTO = 사용자가 입력받는 값, 응답할 값. (사용자한테 보여줄 값만.)
Entity = DB의 한 row. 

